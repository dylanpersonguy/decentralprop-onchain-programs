[package]
name = "batch"
version = "0.1.0"
description = "DecentralProp batch_program — immutable hourly Merkle-root commitments"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "batch"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[lints]
workspace = true

[dependencies]
anchor-lang = "0.31.1"
solana-security-txt = "1.1.3"
